# 桌次管理查素食 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓桌次管理頁能查到素食者的人選、桌次與份數，並讓給餐廳的份數把來賓算進去。

**Architecture:** 後端補資料（`getSeatingBoard` 的每個 seat 帶 `veg`、來賓表加「葷素」欄）→ 前端純邏輯算彙總（`assets/seating.js`，`node --test` 可測）→ UI 三處呈現（既有磚與明細加標記、新的第三顆 pill、常駐彙總卡）→ 兩份 Excel 帶上素食註記。

**Tech Stack:** Google Apps Script（後端，`clasp` 部署）／原生 JS（前端，無框架、無建置）／`node --test`（單元測試）／`xlsx-js-style`＋JSZip（Excel）

## Global Constraints

- **所有 UI 文案繁體中文。**
- **不新增顏色系統**：素食標記沿用既有小標 class 的形狀語彙（`.gtag`／`.chg`／`.tno`），不引入新色票。
- **葷素判定只有一套**：`stats.js` 的 `isVegDiet()`。任何地方要判斷葷素一律呼叫它，不得再寫 `=== '素食'`。
- **上線維護期**（8/28 活動要用）：兩個 repo 各**從 `main`** 開 `feat/veg-seating` 分支，測試綠才合併。
  ⚠️ `jdc-line-gas` 目前工作樹停在 `feat/checkin-station-header`（領先 main 一個 commit、未合併未部署），**不要從那裡開分支**。
- **部署順序**：先 GAS、後前端。順序反了會顯示「素食 0 份」這種看起來正常的錯誤數字。
- 測試指令：
  - GAS：`cd ~/Projects/jdc-line-gas && node --test line-platform/tests/*.test.js`
  - 前端：`cd ~/Projects/jdc-line-liff && node --test tests/*.test.js`
- 每個 commit 訊息結尾加：
  ```
  Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
  ```

### 一個已查證的環境限制（影響 Task 1、Task 2 的作法）

GAS 的各 `.js` 在**線上是共用全域**，但在 `node --test` 下是**各自獨立的 module**——跨檔呼叫在測試環境會 `ReferenceError`。查證結果：本 repo 的純邏輯檔（`stats.js`／`answers.js`／`activity.js`…）**彼此零互相呼叫**，就是為了避開這件事。

因此：
- `isVegDiet()` 放在 **`stats.js`**（葷素判定現在唯一的家）並 `export` → `stats.test.js` 直接 require 得到。
- `Code.js` 呼叫 `isVegDiet()` 走 **GAS 線上全域**（`Code.js` 沒有 export、也沒有單元測試，本來就是這個狀態）。這條路徑 `node --test` 驗不到，所以 **Task 2 的驗收在部署後的 live 實測**（見 Task 2 步驟 6），不能只看測試綠。

---

## File Structure

| 檔案 | 責任 | 動作 |
|---|---|---|
| `jdc-line-gas/line-platform/stats.js` | 葷素判定的唯一來源＋活動統計 | 修改（抽出 `isVegDiet`） |
| `jdc-line-gas/line-platform/tests/stats.test.js` | 上者的測試 | 修改（加 `isVegDiet` 測試） |
| `jdc-line-gas/line-platform/Code.js` | GAS 端點與資料存取 | 修改（`GUEST_HEADER`／`addGuests`／`getSeatingBoard`） |
| `jdc-line-liff/assets/seating.js` | 座位純邏輯（前端與 `node --test` 共用） | 修改（加 `expandGuestRow`、`vegSummary`；`buildSeatingAoa`／`buildFormalAoa` 帶素食） |
| `jdc-line-liff/tests/seating.test.js` | 上者的測試 | 修改 |
| `jdc-line-liff/stats.html` | 桌次管理 UI | 修改（磚、明細、第三顆 pill、彙總卡、`upGuests`） |
| `jdc-line-liff/tests/manual/veg-fixture.js` | UI 驗收用的假資料（Playwright 攔截 `/exec` 回這包） | **新建** |

---

## Task 1: 葷素判定抽成唯一來源（`isVegDiet`）

**Repo:** `jdc-line-gas`　**分支：** 從 `main` 開 `feat/veg-seating`

**Files:**
- Modify: `line-platform/stats.js:46`（`computeActivityStats` 內聯判定）＋檔尾 exports
- Test: `line-platform/tests/stats.test.js`

**Interfaces:**
- Produces: `isVegDiet(v: any) => boolean` —— Task 2 的 `Code.js` 會以 GAS 全域呼叫它。

- [ ] **Step 1: 先開分支**

```bash
cd ~/Projects/jdc-line-gas
git checkout main && git checkout -b feat/veg-seating
git status --short   # 應為空
```

- [ ] **Step 2: 寫失敗的測試**

加到 `line-platform/tests/stats.test.js` 檔尾：

```javascript
const { isVegDiet } = require('../stats.js');

test('isVegDiet：只認 trim 後完全等於「素食」', () => {
  assert.equal(isVegDiet('素食'), true);
  assert.equal(isVegDiet('  素食  '), true);
  assert.equal(isVegDiet('葷食'), false);
  assert.equal(isVegDiet(''), false);
  assert.equal(isVegDiet(null), false);
  assert.equal(isVegDiet(undefined), false);
  // 不做模糊比對：包含比對會把這兩個都判成素食
  assert.equal(isVegDiet('素食可'), false);
  assert.equal(isVegDiet('不吃素'), false);
});
```

- [ ] **Step 3: 跑測試確認它失敗**

```bash
cd ~/Projects/jdc-line-gas && node --test line-platform/tests/stats.test.js
```

Expected: FAIL —— `TypeError: isVegDiet is not a function`

- [ ] **Step 4: 實作**

`line-platform/stats.js`，在 `computeActivityStats` **之前**加：

```javascript
/**
 * 葷素判定的**唯一**來源（回覆表「葷素」欄與來賓表「葷素」欄共用）。
 * 只認 trim 後完全等於「素食」——刻意不做包含比對，否則「素食可」「不吃素」會被判成素食。
 * Code.js 的 getSeatingBoard 走 GAS 全域呼叫這支（node --test 驗不到那條路，見 plan 的環境限制段）。
 */
function isVegDiet(v) {
  return String(v == null ? '' : v).trim() === '素食';
}
```

把 `stats.js:46` 那行改成：

```javascript
          if (isVegDiet(r['葷素'])) counts.veg++; else counts.meat++;
```

檔尾 exports 改成：

```javascript
if (typeof module !== 'undefined' && module.exports) {
  module.exports = { computeActivityStats, isVegDiet };
}
```

- [ ] **Step 5: 跑全部 GAS 測試**

```bash
cd ~/Projects/jdc-line-gas && node --test line-platform/tests/*.test.js
```

Expected: PASS，且**既有的 `computeActivityStats` 測試仍全綠**——那些測試就是這次重構的對照組，證明行為沒被改掉。

- [ ] **Step 6: Commit**

```bash
cd ~/Projects/jdc-line-gas
git add line-platform/stats.js line-platform/tests/stats.test.js
git commit -m "refactor(stats): 葷素判定抽成 isVegDiet，成為唯一來源

Code.js 的 getSeatingBoard 要用同一套判定；內聯的 === '素食' 散在多處
日後改判定會漏改。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 2: 後端補齊素食資料（來賓「葷素」欄＋`seats.veg`＋`vegReady`）

**Repo:** `jdc-line-gas`

**Files:**
- Modify: `line-platform/Code.js:3631`（`GUEST_HEADER`）
- Modify: `line-platform/Code.js:3704-3720`（`addGuests` 的 `pick()`）
- Modify: `line-platform/Code.js:3858`（同仁 `seats.push`）、`:3876`（來賓 `seats.push`）、`:3883`（回傳物件）

**Interfaces:**
- Consumes: `isVegDiet()`（Task 1，GAS 全域）
- Produces: `getSeatingBoard()` 回傳的 `seats[].veg: boolean` 與頂層 `vegReady: true` —— Task 3～8 全部依賴這兩個。
- Produces: `addGuests()` 的每列 `rowsJson` 多收一個欄位 `veg: boolean` —— Task 3 的前端會送。

- [ ] **Step 1: 來賓表加「葷素」欄**

`Code.js:3631`：

```javascript
var GUEST_HEADER = ['活動ID', '負責人員', '廠商名稱', '席位序號', '工程類別', '公司全名', '聯絡人', '聯繫電話', '葷素', '註記', '桌次', '桌次異動時間', '更新時間'];
```

**不必手動改試算表**：`getGuestSheet_`（`Code.js:3634`）已有自癒——缺的欄自動補到最右，且全檔一律照欄名存取、不依賴順序。

- [ ] **Step 2: `addGuests` 收 `veg`**

`Code.js` 的 `pick()`（約 3704 行）在 `case '註記':` 之前加一行：

```javascript
        case '葷素':       return g.veg ? '素食' : '';
```

- [ ] **Step 3: `getSeatingBoard` 的兩處 `seats.push` 帶 `veg`**

同仁（約 3858 行）——回覆表本來就有「葷素」欄：

```javascript
    seats.push({ kind: 'emp', id: String(o['User ID']), name: nm,
                 unit: String(o['單位'] || '') || (unitOf[k] || ''), title: titleOf[k] || '',
                 empNo: empNoOf[k] || '', table: String(o['桌次'] || ''),
                 veg: isVegDiet(o['葷素']),
                 changed: (o['桌次異動時間'] instanceof Date) });
```

來賓（約 3876 行）：

```javascript
    seats.push({ kind: 'guest', id: String(o._row), name: String(o['廠商名稱'] || ''),
                 unit: String(o['負責人員'] || ''), title: '', empNo: '',
                 note: String(o['註記'] || ''), table: String(o['桌次'] || ''),
                 veg: isVegDiet(o['葷素']),
                 changed: (o['桌次異動時間'] instanceof Date) });
```

- [ ] **Step 4: 回傳加 `vegReady`**

`Code.js:3883` 的 return：

```javascript
  return { ok: true, who: auth.who, actId: actId, actName: actName,
           published: published, pubState: pubState,
           vegReady: true,
           ranks: parseTitleRanks(getOptionsSheet_().getDataRange().getValues()),
           seats: seats,
           guestVendors: Object.keys(vendors).length, guestSeats: guestSeats };
```

**為何要這個旗標**：前端先上版而後端沒上時，`s.veg` 全是 `undefined` → 畫面會顯示「素食 0 份」。那是一個看起來完全正常、但會讓人少訂餐點的數字，比報錯更危險。前端讀不到 `vegReady` 就明寫「素食資料尚未就緒」（Task 7）。

- [ ] **Step 5: 語法檢查＋跑測試**

```bash
cd ~/Projects/jdc-line-gas
node --check line-platform/Code.js && echo "語法 OK"
node --test line-platform/tests/*.test.js
```

Expected: 語法 OK、測試全綠。
⚠️ **測試綠不代表這個 Task 對了**——`Code.js` 沒有單元測試，`isVegDiet` 的 GAS 全域解析這條路 `node --test` 走不到。真正的驗收在 Step 6。

- [ ] **Step 6: Commit（部署與 live 驗證併入 Task 9）**

```bash
cd ~/Projects/jdc-line-gas
git add line-platform/Code.js
git commit -m "feat(seating): 來賓表加葷素欄，桌次管理資料源帶 veg 與 vegReady

同仁葷素本來就在回覆表、只是沒被帶進 getSeatingBoard；來賓從來沒有這個欄位。
vegReady 是給前端判斷「後端上了沒」用的，避免顯示看似正常的素食 0 份。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 3: 來賓上傳檔的「素食人數」展開（`expandGuestRow`）

**Repo:** `jdc-line-liff`　**分支：** 從 `main` 開 `feat/veg-seating`（spec 已 commit 在這個分支上，直接接續）

**Files:**
- Modify: `assets/seating.js`（新增 `expandGuestRow` 與 export）
- Modify: `stats.html` 的 `upGuests()`（讀「素食人數」欄、改用 `expandGuestRow`）
- Test: `tests/seating.test.js`

**Interfaces:**
- Produces: `expandGuestRow(count: any, vegCount: any) => Array<{seatNo:number, veg:boolean}>`

⚠️ **兩張表的形狀不同，欄名不可共用**：來賓表（系統存的）是**一席一列**、欄名「葷素」（值 `素食`／空）；來賓上傳檔（使用者手上的 Excel）是**一列一廠商**＋`參加人數`、欄名「**素食人數**」（整數）。

- [ ] **Step 1: 開分支（spec 已在此分支）**

```bash
cd ~/Projects/jdc-line-liff
git branch --show-current   # 應為 feat/veg-seating；若不是：git checkout feat/veg-seating
```

- [ ] **Step 2: 寫失敗的測試**

加到 `tests/seating.test.js` 檔尾（並把 `expandGuestRow` 加進檔頭的 require 解構）：

```javascript
test('expandGuestRow：5 人 2 素 → 席位 1、2 為素食', () => {
  assert.deepEqual(expandGuestRow(5, 2), [
    { seatNo: 1, veg: true }, { seatNo: 2, veg: true },
    { seatNo: 3, veg: false }, { seatNo: 4, veg: false }, { seatNo: 5, veg: false },
  ]);
});

test('expandGuestRow：素食人數超過參加人數 → 取小值，不得產生比席位多的素食', () => {
  assert.deepEqual(expandGuestRow(2, 5), [
    { seatNo: 1, veg: true }, { seatNo: 2, veg: true },
  ]);
});

test('expandGuestRow：舊檔沒有素食人數欄 → 全部 false', () => {
  assert.deepEqual(expandGuestRow(3, undefined), [
    { seatNo: 1, veg: false }, { seatNo: 2, veg: false }, { seatNo: 3, veg: false },
  ]);
  assert.deepEqual(expandGuestRow(3, ''), [
    { seatNo: 1, veg: false }, { seatNo: 2, veg: false }, { seatNo: 3, veg: false },
  ]);
});

test('expandGuestRow：參加人數 0 → 一列 seatNo 0（不佔位但保留這家廠商）', () => {
  assert.deepEqual(expandGuestRow(0, 0), [{ seatNo: 0, veg: false }]);
  assert.deepEqual(expandGuestRow('', 0), [{ seatNo: 0, veg: false }]);
});

test('expandGuestRow：負數素食人數當 0', () => {
  assert.deepEqual(expandGuestRow(2, -1), [
    { seatNo: 1, veg: false }, { seatNo: 2, veg: false },
  ]);
});
```

- [ ] **Step 3: 跑測試確認它失敗**

```bash
cd ~/Projects/jdc-line-liff && node --test tests/seating.test.js
```

Expected: FAIL —— `TypeError: expandGuestRow is not a function`

- [ ] **Step 4: 實作**

`assets/seating.js`，加在 `expandGuests` 之後：

```javascript
/**
 * 來賓上傳檔一列（＝一家廠商）→ 席位陣列。
 * 上傳檔是一列一廠商（參加人數 n），來賓表是一席一列，所以要在這裡展開。
 * 素食人數 v 上限為 n，落在**席位序號最小的前 v 席**——同一家人分不出誰是誰，
 * 對「哪一桌幾份」這個用途足夠；同家被拆兩桌時的誤差由 vegSummary 提示（見該函式）。
 * n<=0 回一列 seatNo:0：不佔位，但保留這家廠商的資料列。
 * @returns {Array<{seatNo:number, veg:boolean}>}
 */
function expandGuestRow(count, vegCount) {
  var n = Number(count) || 0;
  if (n <= 0) return [{ seatNo: 0, veg: false }];
  var v = Math.max(0, Math.min(Number(vegCount) || 0, n));
  var out = [];
  for (var i = 1; i <= n; i++) out.push({ seatNo: i, veg: i <= v });
  return out;
}
```

檔尾 `module.exports` 加入 `expandGuestRow`。

- [ ] **Step 5: 跑測試確認通過**

```bash
cd ~/Projects/jdc-line-liff && node --test tests/seating.test.js
```

Expected: PASS

- [ ] **Step 6: `upGuests()` 改用它**

`stats.html` 的 `upGuests()`，把原本這段：

```javascript
      var n=Number(g[ix('參加人數')])||0;
      if(n<=0)rows.push(Object.assign({seatNo:0},base));
      else for(var k=1;k<=n;k++)rows.push(Object.assign({seatNo:k},base));
```

換成：

```javascript
      var vc=(ix('素食人數')>=0?g[ix('素食人數')]:0);
      expandGuestRow(g[ix('參加人數')],vc).forEach(function(sp){
        rows.push(Object.assign({seatNo:sp.seatNo,veg:sp.veg},base));
      });
```

⚠️ 不要改 Step 2 之前那段的欄位必要性檢查——「素食人數」是**選填**，舊格式檔案必須照收（`ix('素食人數')` 回 -1 時走 `0`）。

- [ ] **Step 7: 語法檢查**

```bash
cd ~/Projects/jdc-line-liff
node -e 'const fs=require("fs"),cp=require("child_process"),os=require("os"),path=require("path");
const html=fs.readFileSync("stats.html","utf8");
const re=/<script(?![^>]*\bsrc=)[^>]*>([\s\S]*?)<\/script>/g;let m,i=0,bad=0;
const tmp=fs.mkdtempSync(path.join(os.tmpdir(),"syn-"));
while((m=re.exec(html))!==null){const p=path.join(tmp,`s${i++}.js`);fs.writeFileSync(p,m[1]);
const r=cp.spawnSync("node",["--check",p],{encoding:"utf8"});if(r.status!==0){bad++;console.error(r.stderr);}}
console.log(bad?"❌ 語法錯誤":"✅ 語法通過");process.exit(bad?1:0);'
```

Expected: `✅ 語法通過`（這段就是 CI `syntax-check.yml` 對 `stats.html` 做的事，本機先跑一次）

- [ ] **Step 8: Commit**

```bash
cd ~/Projects/jdc-line-liff
git add assets/seating.js tests/seating.test.js stats.html
git commit -m "feat(seating): 來賓上傳檔支援「素食人數」欄

展開邏輯從 stats.html 內聯抽成 expandGuestRow（原本沒有任何測試）。
素食人數是選填，舊格式檔案照收當 0。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 4: 素食彙總純邏輯（`vegSummary`）

**Repo:** `jdc-line-liff`

**Files:**
- Modify: `assets/seating.js`
- Test: `tests/seating.test.js`

**Interfaces:**
- Consumes: `seats[].veg`（Task 2）、既有的 `tableOrder_`（`seating.js:287`，已把空桌次排最後）
- Produces: `vegSummary(seats) => {rows: Array<{table:string,count:number,names:string[]}>, total:number, splitVendors:string[]}` —— Task 7（彙總卡）與 Task 8（Excel）都用它

- [ ] **Step 1: 寫失敗的測試**

加到 `tests/seating.test.js` 檔尾（`vegSummary` 加進檔頭 require 解構）：

```javascript
test('vegSummary：只列有素食的桌，依桌號排序，未排桌殿後', () => {
  const seats = [
    { kind: 'emp', name: '甲', table: '3', veg: true },
    { kind: 'emp', name: '乙', table: '3', veg: false },
    { kind: 'emp', name: '丙', table: '1', veg: true },
    { kind: 'emp', name: '丁', table: '2', veg: false },   // 2 桌零素食 → 不得出現
    { kind: 'emp', name: '戊', table: '', veg: true },     // 未排桌
  ];
  const r = vegSummary(seats);
  assert.deepEqual(r.rows, [
    { table: '1', count: 1, names: ['丙'] },
    { table: '3', count: 1, names: ['甲'] },
    { table: '', count: 1, names: ['戊'] },
  ]);
  assert.equal(r.total, 3);
});

test('vegSummary：同桌多份彙總成一行', () => {
  const r = vegSummary([
    { kind: 'emp', name: '甲', table: '5', veg: true },
    { kind: 'emp', name: '乙', table: '5', veg: true },
  ]);
  assert.deepEqual(r.rows, [{ table: '5', count: 2, names: ['甲', '乙'] }]);
  assert.equal(r.total, 2);
});

test('vegSummary：同一家廠商的素食席位落在兩桌 → 列入 splitVendors 提示', () => {
  const r = vegSummary([
    { kind: 'guest', name: '某某工程行', table: '1', veg: true },
    { kind: 'guest', name: '某某工程行', table: '2', veg: true },
    { kind: 'guest', name: '另一家', table: '1', veg: true },
  ]);
  assert.deepEqual(r.splitVendors, ['某某工程行']);
  assert.equal(r.total, 3);
});

test('vegSummary：同一家廠商素食都在同一桌 → 不提示', () => {
  const r = vegSummary([
    { kind: 'guest', name: '某某工程行', table: '1', veg: true },
    { kind: 'guest', name: '某某工程行', table: '1', veg: true },
  ]);
  assert.deepEqual(r.splitVendors, []);
});

test('vegSummary：全部無素食 → 空清單、合計 0', () => {
  const r = vegSummary([{ kind: 'emp', name: '甲', table: '1', veg: false }]);
  assert.deepEqual(r.rows, []);
  assert.equal(r.total, 0);
  assert.deepEqual(r.splitVendors, []);
});

test('vegSummary：veg 欄不存在（後端未上版）→ 不當成素食', () => {
  const r = vegSummary([{ kind: 'emp', name: '甲', table: '1' }]);
  assert.equal(r.total, 0);
});
```

- [ ] **Step 2: 跑測試確認它失敗**

```bash
cd ~/Projects/jdc-line-liff && node --test tests/seating.test.js
```

Expected: FAIL —— `TypeError: vegSummary is not a function`

- [ ] **Step 3: 實作**

`assets/seating.js`，加在 `tableOrder_` 之後（它依賴 `tableOrder_`）：

```javascript
/**
 * 素食彙總：一行一桌的份數與名單，給餐廳報數用。
 * 只回**有素食**的桌（零素食的桌不列，避免整片「0 份」的噪音）；未排桌的集中成 table:'' 一行、
 * 靠 tableOrder_ 排在最後——那一行是給使用者看的缺口（還沒定位的份數），不是給餐廳的。
 * splitVendors＝同一家廠商的素食席位落在兩張以上的桌。展開時素食固定落在席位序號最小的前幾席
 * （expandGuestRow），同家被拆桌時份數可能算錯桌，所以要在畫面上提示使用者手調。
 * @returns {{rows:Array<{table:string,count:number,names:string[]}>, total:number, splitVendors:string[]}}
 */
function vegSummary(seats) {
  var byTable = {}, keys = [], total = 0, vendorTables = {};
  (seats || []).forEach(function (s) {
    if (!s || !s.veg) return;
    var t = String(s.table || '');
    if (!byTable[t]) { byTable[t] = []; keys.push(t); }
    byTable[t].push(String(s.name || ''));
    total++;
    if (s.kind === 'guest' && t) {
      var v = String(s.name || '');
      (vendorTables[v] = vendorTables[v] || {})[t] = true;
    }
  });
  keys.sort(tableOrder_);
  return {
    rows: keys.map(function (t) {
      return { table: t, count: byTable[t].length, names: byTable[t].slice() };
    }),
    total: total,
    splitVendors: Object.keys(vendorTables).filter(function (v) {
      return Object.keys(vendorTables[v]).length > 1;
    }),
  };
}
```

檔尾 `module.exports` 加入 `vegSummary`。

- [ ] **Step 4: 跑全部前端測試**

```bash
cd ~/Projects/jdc-line-liff && node --test tests/*.test.js
```

Expected: PASS（既有測試全綠，證明沒動到別人）

- [ ] **Step 5: Commit**

```bash
cd ~/Projects/jdc-line-liff
git add assets/seating.js tests/seating.test.js
git commit -m "feat(seating): 素食彙總純邏輯 vegSummary

一行一桌的份數與名單；未排桌單獨成行；同家廠商跨桌時回 splitVendors 供畫面提示。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 5: 建立 UI 驗收 harness ＋ 磚與明細列的素食標記

**Repo:** `jdc-line-liff`

UI 改動不能只靠單元測試與靜態審查（memory `feedback_ui_change_needs_real_page`：三輪審查＋四項靜態確認都沒抓到的下拉隱藏，使用者開一次就看到）。所以這個 Task 先把驗收 harness 建起來，Task 6、7 沿用。

**Files:**
- Create: `tests/manual/veg-fixture.js`（假的 `getSeatingBoard` 回應）
- Create: `tests/manual/veg-ui.mjs`（Playwright 腳本：攔截 `/exec`、開頁、截圖、印 console）
- Modify: `stats.html`（`renderBoard` 的兩處磚、`renderDetail`）
- Modify: `stats.html` 的 CSS（`.prow .veg` 小標）

**Interfaces:**
- Consumes: `SB.seats[].veg`、`SB.vegReady`（Task 2）

- [ ] **Step 1: 建立假資料**

`tests/manual/veg-fixture.js`：

```javascript
/** UI 驗收用假資料。刻意涵蓋：零素食的桌、同桌多素食、未排桌素食、跨兩桌的同名廠商。 */
module.exports = {
  ok: true, who: '洪炫佑', actId: 'actTEST', actName: '驗收用活動',
  published: false, pubState: 'none', vegReady: true,
  ranks: { '副理': 10, '課長': 20 },
  guestVendors: 2, guestSeats: 3,
  seats: [
    { kind: 'emp', id: 'U1', name: '甲一', unit: '管理部', title: '副理', empNo: 'A01', table: '1', veg: true,  changed: false },
    { kind: 'emp', id: 'U2', name: '乙二', unit: '管理部', title: '課長', empNo: 'A02', table: '1', veg: false, changed: false },
    { kind: 'emp', id: 'U3', name: '丙三', unit: '工務部', title: '',     empNo: 'A03', table: '1', veg: true,  changed: true  },
    { kind: 'emp', id: 'U4', name: '丁四', unit: '工務部', title: '',     empNo: 'A04', table: '2', veg: false, changed: false },
    { kind: 'emp', id: 'U5', name: '戊五', unit: '工務部', title: '',     empNo: 'A05', table: '',  veg: true,  changed: false },
    { kind: 'guest', id: '2', name: '某某工程行', unit: '甲一', title: '', empNo: '', note: '', table: '1', veg: true,  changed: false },
    { kind: 'guest', id: '3', name: '某某工程行', unit: '甲一', title: '', empNo: '', note: '', table: '2', veg: true,  changed: false },
    { kind: 'guest', id: '4', name: '另一家',     unit: '乙二', title: '', empNo: '', note: '', table: '2', veg: false, changed: false },
  ],
};
```

預期彙總：1 桌 3 份（甲一、丙三、某某工程行）／2 桌 1 份（某某工程行）／未排桌 1 份（戊五）／合計 5／`splitVendors=['某某工程行']`。

- [ ] **Step 2: 建立 Playwright 驗收腳本**

`tests/manual/veg-ui.mjs`：

```javascript
/**
 * UI 驗收：攔掉所有 GAS 請求、餵 veg-fixture，走頁面自己的啟動流程（不手動注入 DOM——
 * 注入會繞過啟動流程，正是 memory feedback_open_page_is_not_verification 的教訓）。
 * 用法：node tests/manual/veg-ui.mjs
 */
import { chromium } from 'playwright';
import { createRequire } from 'node:module';
const require = createRequire(import.meta.url);
const FIXTURE = require('./veg-fixture.js');

const browser = await chromium.launch();
const page = await browser.newPage({ viewport: { width: 390, height: 844 } });  // iPhone 尺寸：他現場用手機

const errors = [];
page.on('console', (m) => { if (m.type() === 'error') errors.push(m.text()); });
page.on('pageerror', (e) => errors.push('pageerror: ' + e.message));

await page.route('**/script.google.com/**', (route) => {
  const action = new URL(route.request().url()).searchParams.get('action');
  const body = { getSeatingBoard: FIXTURE, listActivities: { ok: true, rows: [{ id: 'actTEST', name: '驗收用活動', status: '開放', open: true, replies: 5 }] } }[action]
    || { ok: true, results: { listActivities: { ok: true, rows: [{ id: 'actTEST', name: '驗收用活動', status: '開放', open: true, replies: 5 }] } } };
  route.fulfill({ status: 200, contentType: 'application/javascript', body: 'cb(' + JSON.stringify(body) + ')' });
});

await page.goto('file://' + process.cwd() + '/stats.html?t=dummy');
await page.click('#tabbtn-tables');
await page.waitForSelector('#seat-board .tile', { timeout: 5000 });
await page.screenshot({ path: '/tmp/veg-01-board.png', fullPage: true });
await page.click('#seat-board .tile');                       // 1 桌
await page.waitForSelector('#seat-detail .prow');
await page.screenshot({ path: '/tmp/veg-02-detail.png', fullPage: true });

console.log('console 錯誤：', errors.length ? errors : '無');
await browser.close();
if (errors.length) process.exit(1);
```

- [ ] **Step 3: 跑一次確認 harness 本身會動（改任何 UI 之前）**

```bash
cd ~/Projects/jdc-line-liff && node tests/manual/veg-ui.mjs
```

Expected: `console 錯誤： 無`，`/tmp/veg-01-board.png` 有 2 個桌磚。
⚠️ **這一步是 harness 的對照組**：先確認它在**還沒改 UI** 的情況下跑得起來、截得到圖。跳過這步，之後畫面沒東西時分不出是 UI 沒做還是 harness 壞了。
若 `playwright` 未安裝：`npx playwright install chromium` 後重跑。

- [ ] **Step 4: 磚上顯示素食數**

`stats.html` 的 `renderBoard()`，**依桌次**那段（約 459-464 行）：

```javascript
    h='<div class="board">'+keys.map(function(t){
      var m=SB.seats.filter(function(s){return String(s.table||'')===t;});
      var dirty=m.some(function(s){return s.changed;});
      var nv=m.filter(function(s){return s.veg;}).length;
      return '<div class="tile'+(seatSel===t?' sel':'')+(dirty?' dirty':'')+'" onclick="pickTile(\''+esc(t)+'\')">'
        +'<b>'+esc(t)+'桌</b><span class="c">'+m.length+'人'+(nv?'・素 '+nv:'')+(dirty?'・有變更':'')+'</span></div>';
    }).join('')+'</div>';
```

**依單位**那段（約 472-476 行）同樣處理：

```javascript
    h='<div class="board">'+uks.map(function(u){
      var dirty=byU[u].some(function(s){return s.changed;});
      var nv=byU[u].filter(function(s){return s.veg;}).length;
      return '<div class="tile unit'+(seatSel===u?' sel':'')+(dirty?' dirty':'')+'" onclick="pickTile(\''+esc(u)+'\')">'
        +'<b>'+esc(u)+'</b><span class="c">'+byU[u].length+'人'+(nv?'・素 '+nv:'')+(dirty?'・有變更':'')+'</span></div>';
    }).join('')+'</div>';
```

沒有素食的磚不顯示這段——避免整片「素 0」的視覺噪音。

- [ ] **Step 5: 明細列加「素」小標**

`stats.html` 的 CSS 區（`.prow .tno` 那行之後，約 98 行）加：

```css
  .prow .veg { font-size: 10.5px; background: #e6f9ee; color: #1a7d44; border-radius: 8px; padding: 0 6px; font-weight: 700; }
```

`renderDetail()` 內，來賓與同仁兩條分支**各**在 `s.changed` 判斷之前插入同一行：

```javascript
      if(s.veg)h+='<span class="veg">素</span>';
```

即來賓分支變成：

```javascript
    if(s.kind==='guest'){
      h+='<span class="gname">'+esc(s.name)+'</span><span class="ti">'+esc(s.note||'')+'</span>';
      if(s.veg)h+='<span class="veg">素</span>';
      if(s.changed)h+='<span class="chg">已變更</span>';
```

同仁分支變成：

```javascript
    }else{
      h+='<span class="nm">'+esc(s.name)+'</span><span class="ti">'+esc(s.title||'')+'</span>';
      if(s.veg)h+='<span class="veg">素</span>';
      if(s.changed)h+='<span class="chg">已變更</span>';
```

- [ ] **Step 6: 跑驗收腳本並實際看圖**

```bash
cd ~/Projects/jdc-line-liff && node tests/manual/veg-ui.mjs
```

Expected: console 零錯誤。**接著用 Read 工具實際打開 `/tmp/veg-01-board.png` 與 `/tmp/veg-02-detail.png` 看**，確認：
- 1 桌磚顯示「4人・素 3」、2 桌磚顯示「3人・素 1」
- 點 1 桌後，明細裡甲一、丙三、某某工程行三列有綠色「素」小標，乙二沒有
- 丙三那列同時有「素」與「已變更」兩個標，沒有擠掉或換行破版

⚠️ 只看 console 沒錯誤**不算驗過**——CSS 破版不會進 console。

- [ ] **Step 7: 語法檢查＋commit**

```bash
cd ~/Projects/jdc-line-liff
node --test tests/*.test.js
git add stats.html tests/manual/veg-fixture.js tests/manual/veg-ui.mjs
git commit -m "feat(stats): 桌次磚與明細列顯示素食（含 UI 驗收 harness）

harness 攔 GAS 請求餵假資料、走頁面自己的啟動流程，不手動注入 DOM。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 6: 第三顆 pill「🥬 素食」——以人為單位、可操作

**Repo:** `jdc-line-liff`

**Files:**
- Modify: `stats.html:183-187`（pills 與 scope-row）
- Modify: `stats.html` 的 `setSeatView()`、`renderBoard()`

**Interfaces:**
- Consumes: `SB.seats[].veg`、既有的 `renderDetail(title, list, showTableTag)`

- [ ] **Step 1: 加第三顆 pill**

`stats.html:183-184`：

```html
    <div class="pills"><button class="pill on" id="pv-table" onclick="setSeatView('table')">依桌次</button>
      <button class="pill" id="pv-unit" onclick="setSeatView('unit')">依單位</button>
      <button class="pill" id="pv-veg" onclick="setSeatView('veg')">🥬 素食</button></div>
```

- [ ] **Step 2: `setSeatView` 認得第三個值**

```javascript
function setSeatView(v){
  seatView=v; seatSel=null;
  document.getElementById('pv-table').className='pill'+(v==='table'?' on':'');
  document.getElementById('pv-unit').className='pill'+(v==='unit'?' on':'');
  document.getElementById('pv-veg').className='pill'+(v==='veg'?' on':'');
  document.getElementById('scope-row').style.display=(v==='table')?'':'none';
  renderBoard();
}
```

⚠️ 原本第 434 行那句設定 `pv-unit` 的 className 要保留——上面這段已含。`scope-row` 只在「依桌次」顯示：素食視圖必須把未排桌的人一起列出來，否則現場會漏人。

- [ ] **Step 3: `renderBoard` 加素食分支**

在 `renderBoard()` 開頭 `if(seatView==='table'){` 之前插入：

```javascript
  if(seatView==='veg'){
    var vs=SB.seats.filter(function(s){return s.veg;});
    box.innerHTML='';
    if(!SB.vegReady){box.innerHTML='<div class="empty">素食資料尚未就緒（後端未更新）</div>';
      document.getElementById('seat-detail').innerHTML='';return;}
    if(!vs.length){box.innerHTML='<div class="empty">這場活動沒有人吃素</div>';
      document.getElementById('seat-detail').innerHTML='';return;}
    renderDetail('🥬 素食 '+vs.length+' 人',vs,true);
    return;
  }
```

`renderDetail` 的第三個參數 `showTableTag=true` → 每列會顯示「N 桌」膠囊，且每列自帶既有的「移桌」「🔳」「⬇QR」動作。未排桌者 `s.table` 為空 → 不顯示膠囊。

- [ ] **Step 4: 未排桌者要看得出來**

`renderDetail()` 裡，`showTableTag` 那兩處判斷各改成（來賓、同仁分支各一處）：

```javascript
      if(showTableTag)h+=s.table?('<span class="tno">'+esc(s.table)+' 桌</span>')
                                :'<span class="chg">未排桌</span>';
```

原本 `if(showTableTag&&s.table)` 會讓未排桌者**什麼都不顯示**——在素食視圖裡看起來就像漏掉了桌號，而不是「還沒排」。沿用 `.chg` 的琥珀色，語意一致（都是「要處理」）。

- [ ] **Step 5: 驗收腳本補這個視圖**

`tests/manual/veg-ui.mjs` 在既有截圖之後加：

```javascript
await page.click('#pv-veg');
await page.waitForSelector('#seat-detail .prow');
await page.screenshot({ path: '/tmp/veg-03-vegview.png', fullPage: true });
```

- [ ] **Step 6: 跑並實際看圖**

```bash
cd ~/Projects/jdc-line-liff && node tests/manual/veg-ui.mjs
```

Expected: console 零錯誤。**用 Read 工具打開 `/tmp/veg-03-vegview.png`**，確認：
- 標題「🥬 素食 5 人」
- 五列：甲一（1 桌）、丙三（1 桌）、戊五（**未排桌**）、某某工程行 ×2（1 桌、2 桌）
- 每列都有「移桌」；同仁列另有「🔳」「⬇QR」
- 三顆 pill 只有「🥬 素食」是選中態，`scope-row` 已隱藏

- [ ] **Step 7: Commit**

```bash
cd ~/Projects/jdc-line-liff
node --test tests/*.test.js
git add stats.html tests/manual/veg-ui.mjs
git commit -m "feat(stats): 第三顆 pill「素食」——以人為單位、可直接移桌

未排桌者在明細裡改顯示「未排桌」標記，原本是什麼都不顯示、看起來像漏資料。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 7: 常駐「🥬 素食彙總」卡——以桌為單位、唯讀

**Repo:** `jdc-line-liff`

**Files:**
- Modify: `stats.html:193`（座位表卡之後插入新卡）
- Modify: `stats.html` 的 `loadSeating()`（載完後渲染）
- Modify: `stats.html` CSS

**Interfaces:**
- Consumes: `vegSummary()`（Task 4）、`SB.vegReady`（Task 2）

- [ ] **Step 1: 加卡片容器**

`stats.html`，在座位表卡的 `</div>` 之後（第 193 行「員工 QR」卡之前）插入：

```html
  <div class="card">
    <div style="font-weight:700;font-size:16px;margin-bottom:4px">🥬 素食彙總 <span id="veg-sum" style="color:#888;font-weight:400;font-size:14px"></span></div>
    <div id="veg-box"><div class="empty">載入中…</div></div>
  </div>
```

- [ ] **Step 2: CSS**

CSS 區加：

```css
  .vrow { display: flex; align-items: center; gap: 10px; padding: 7px 2px; border-bottom: 1px solid #f2f2f2; font-size: 14px; }
  .vrow:last-child { border-bottom: none; }
  .vrow .vt { font-weight: 700; min-width: 60px; }
  .vrow .vc { background: #e6f9ee; color: #1a7d44; border-radius: 8px; padding: 0 8px; font-weight: 700; font-size: 13px; }
  .vrow .vn { font-size: 12.5px; color: #777; flex: 1; }
  .vtot { display: flex; justify-content: space-between; border-top: 2px solid #1a5c3a; margin-top: 6px; padding-top: 7px; font-weight: 700; }
  .vwarn { font-size: 12.5px; color: #7a5320; background: #fff8e8; border-radius: 6px; padding: 6px 9px; margin-top: 8px; }
```

- [ ] **Step 3: 渲染函式**

在 `renderBoard()` 之後加：

```javascript
function renderVegSummary(){
  var box=document.getElementById('veg-box'),sum=document.getElementById('veg-sum');
  if(!SB){box.innerHTML='<div class="empty">請先選活動</div>';sum.textContent='';return;}
  if(!SB.vegReady){
    box.innerHTML='<div class="empty">素食資料尚未就緒（後端未更新）</div>';sum.textContent='';return;}
  var v=vegSummary(SB.seats);
  if(!v.total){box.innerHTML='<div class="empty">這場活動沒有人吃素</div>';sum.textContent='';return;}
  sum.textContent='合計 '+v.total+' 份';
  var h=v.rows.map(function(r){
    return '<div class="vrow"><span class="vt">'+(r.table?esc(r.table)+' 桌':'未排桌')+'</span>'
      +'<span class="vc">'+r.count+' 份</span>'
      +'<span class="vn">'+esc(r.names.join('、'))+'</span></div>';
  }).join('');
  h+='<div class="vtot"><span>合計</span><span>'+v.total+' 份</span></div>';
  if(v.splitVendors.length){
    h+='<div class="vwarn">⚠️ '+esc(v.splitVendors.join('、'))
      +' 的席位被拆到兩桌以上，素食份數是算在席位序號較前的那桌，可能與實際不符——請確認後用「移桌」調整。</div>';
  }
  box.innerHTML=h;
}
```

- [ ] **Step 4: 接到載入流程**

`loadSeating()` 內，`renderBoard();` 那一行之後加 `renderVegSummary();`。
同時在 `loadSeating()` 開頭清空的那段（設 `seat-board` 為「載入中…」處）加：

```javascript
  document.getElementById('veg-box').innerHTML='<div class="empty">載入中…</div>';
```

⚠️ `moveSeat()` 成功後會呼叫 `loadSeating()`，所以移桌之後彙總會自動跟著更新——不需要另外掛。

- [ ] **Step 5: 驗收腳本補截圖**

`tests/manual/veg-ui.mjs` 加：

```javascript
await page.waitForSelector('#veg-box .vrow');
await page.locator('#veg-box').screenshot({ path: '/tmp/veg-04-summary.png' });
```

- [ ] **Step 6: 跑並實際看圖**

```bash
cd ~/Projects/jdc-line-liff && node tests/manual/veg-ui.mjs
```

Expected: console 零錯誤。**用 Read 工具打開 `/tmp/veg-04-summary.png`**，確認：
- 三行：`1 桌　3 份　甲一、丙三、某某工程行`／`2 桌　1 份　某某工程行`／`未排桌　1 份　戊五`
- 合計 5 份（卡片標題旁與末行都要有）
- 底部琥珀色提示點名「某某工程行」
- 在 390px 寬度下姓名那欄沒有把版面撐爆（名字多時應該擠壓或換行，不是水平捲動）

- [ ] **Step 7: Commit**

```bash
cd ~/Projects/jdc-line-liff
node --test tests/*.test.js
git add stats.html tests/manual/veg-ui.mjs
git commit -m "feat(stats): 常駐素食彙總卡（一行一桌的份數，給餐廳報數用）

vegReady 為假時明寫「尚未就緒」，不顯示任何數字——素食 0 份比報錯更危險。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 8: 兩份 Excel 帶素食

**Repo:** `jdc-line-liff`

**Files:**
- Modify: `assets/seating.js`（`groupSeatCategories`、`buildSeatingAoa`、`buildFormalAoa`）
- Test: `tests/seating.test.js`

**Interfaces:**
- Consumes: `seats[].veg`、`vegSummary()`
- 既有行為（不可破壞）：`parseSeatingUpload` 的 `clean()` 會去掉 `（…）` 註記（`seating.js:235`），所以排位用檔上的「（素）」上傳回來不會被當成別人。

- [ ] **Step 1: 寫失敗的測試**

```javascript
test('groupSeatCategories：素食者姓名帶「（素）」後綴', () => {
  const seats = [
    { kind: 'emp', name: '甲一', unit: '管理部', title: '副理', veg: true },
    { kind: 'emp', name: '乙二', unit: '管理部', title: '課長', veg: false },
    { kind: 'guest', name: '某某工程行', unit: '甲一', veg: true },
  ];
  const g = groupSeatCategories(seats, [{ type: '單位', name: '管理部', group: '總公司' }], { '副理': 10, '課長': 20 });
  assert.deepEqual(g.byUnit['管理部'], ['甲一（素）', '乙二']);
  assert.deepEqual(g.guestsByOwner['甲一'], ['某某工程行（素）']);
});

test('buildFormalAoa：正式座位表的座位格不標素食（拍板：只要哪桌幾份）', () => {
  const { aoa } = buildFormalAoa([{ kind: 'emp', name: '甲一', unit: '管理部', table: '1', veg: true }], {});
  assert.equal(aoa[1][1], '甲一');
});

test('buildFormalAoa：表格最下方附素食彙總（桌號／份數／姓名）', () => {
  const { aoa } = buildFormalAoa([
    { kind: 'emp', name: '甲一', unit: '管理部', table: '1', veg: true },
    { kind: 'emp', name: '乙二', unit: '管理部', table: '1', veg: false },
    { kind: 'emp', name: '丙三', unit: '工務部', table: '2', veg: true },
  ], {});
  const flat = aoa.map((r) => r.join('|'));
  assert.ok(flat.some((r) => r.indexOf('素食彙總') >= 0), '應有「素食彙總」標題列');
  assert.ok(flat.some((r) => r.indexOf('甲一') >= 0 && r.indexOf('1') >= 0));
  assert.ok(flat.some((r) => r.indexOf('合計') >= 0 && r.indexOf('2') >= 0));
});

test('buildFormalAoa：零素食時不附彙總段（不留空標題）', () => {
  const { aoa } = buildFormalAoa([{ kind: 'emp', name: '甲一', unit: '管理部', table: '1', veg: false }], {});
  assert.ok(!aoa.map((r) => r.join('|')).some((r) => r.indexOf('素食彙總') >= 0));
});
```

- [ ] **Step 2: 跑測試確認它失敗**

```bash
cd ~/Projects/jdc-line-liff && node --test tests/seating.test.js
```

Expected: FAIL（4 個新測試紅）

- [ ] **Step 3: `groupSeatCategories` 加後綴**

`seating.js` 的 `toNames`（約 164 行）改成：

```javascript
  var toNames = function (m) {
    Object.keys(m).forEach(function (k) {
      m[k] = sortSeats(m[k], ranks || {}).map(function (x) {
        return x.veg ? (x.name + '（素）') : x.name;
      });
    });
  };
```

排位時看得到誰吃素，才決定要集中還是分散。上傳排位結果時 `parseSeatingUpload` 會把括號註記清掉，整個名字複製到座位格不會誤判。

- [ ] **Step 4: `buildFormalAoa` 底部加彙總段**

在 `aoa.push(['人數'].concat(...))` 那行**之後**、`return` 之前加：

```javascript
  // 正式座位表底部附素食彙總：那份本來就是要印出來給人看的，
  // 彙總長在同一張紙上，比另開一份檔案可靠（另開的一定會有人忘記帶）。
  var vs = vegSummary(seats);
  if (vs.total) {
    aoa.push([]);
    aoa.push(['素食彙總']);
    aoa.push(['桌次', '份數', '姓名']);
    vs.rows.forEach(function (r) {
      aoa.push([r.table || '未排桌', r.count, r.names.join('、')]);
    });
    aoa.push(['合計', vs.total, '']);
  }
```

⚠️ `buildFormalAoa` 的 `fills`／`guestCells` 是用**列索引**定位的，而這段是 append 在最後 → 不影響既有座位格的座標。`opts.boldLastRow` 會把最後一列（合計）加粗，語意剛好。

- [ ] **Step 5: 跑測試確認通過**

```bash
cd ~/Projects/jdc-line-liff && node --test tests/*.test.js
```

Expected: PASS（含既有的 `buildSeatingAoa`／`buildFormalAoa` 測試全綠——它們是這次改動的對照組）

- [ ] **Step 6: 真的產一次檔案來看**

單元測試只驗 AOA 陣列，驗不到 Excel 寫出來長什麼樣（memory `feedback_sheetjs_xlsx_writer_traps`：公式變文字、空白格帶樣式被丟）。在 `tests/manual/veg-ui.mjs` 尾端加：

```javascript
const dl = page.waitForEvent('download');
await page.click('button[onclick="dlFormal()"]');
const f = await dl;
await f.saveAs('/tmp/veg-formal.xlsx');
console.log('正式座位表已存 /tmp/veg-formal.xlsx');
```

跑完用 Python 開來確認彙總段真的在裡面：

```bash
cd ~/Projects/jdc-line-liff && node tests/manual/veg-ui.mjs
python3 -c "
import openpyxl
ws = openpyxl.load_workbook('/tmp/veg-formal.xlsx').active
for row in ws.iter_rows(values_only=True):
    if any(v not in (None, '') for v in row): print(row)
"
```

Expected: 看得到座位格、然後空行、`('素食彙總',)`、`('桌次','份數','姓名')`、`('1', 3, '甲一、丙三、某某工程行')`…`('合計', 5, '')`。

- [ ] **Step 7: Commit**

```bash
cd ~/Projects/jdc-line-liff
git add assets/seating.js tests/seating.test.js tests/manual/veg-ui.mjs
git commit -m "feat(seating): 排位用檔標（素）、正式座位表底部附素食彙總

排位用檔的括號註記上傳時會被 parseSeatingUpload 清掉（既有行為），不會誤判成別人。
正式座位表的座位格不標（拍板：只要哪桌幾份）。

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 9: 部署與 live 驗收

⚠️ **順序不可顛倒**。先 GAS、後前端；反了會顯示「素食 0 份」這種看起來正常的錯誤數字。

**REQUIRED SUB-SKILL:** 這個 Task 的 live 驗證用 `line-backend-test` skill（JSONP 呼叫姿勢、哨兵資料、副作用驗證、部署紀律都在那支裡）。

- [ ] **Step 1: 合併 GAS 分支並部署**

```bash
cd ~/Projects/jdc-line-gas
node --test line-platform/tests/*.test.js          # 綠才往下
git checkout main && git merge --no-ff feat/veg-seating
git push
```

部署走 `sync-to-gas.yml`（push main 觸發）。
⚠️ **看 Actions 的 step 是 success 還是 skipped**——memory `feedback_ci_nonascii_filename_skip`：中文檔名會讓 path 偵測失準、deploy step 被 skipped 而 job 仍然顯示綠燈。

- [ ] **Step 2: live 驗證後端（附對照組）**

用 board token 打 `getSeatingBoard`，確認 `vegReady:true` 且 `seats[].veg` 有 `true`：

```bash
curl -s "<GAS_URL>?action=getSeatingBoard&token=<BOARD_TOKEN>&actId=midyear2026&callback=cb" \
  | python3 -c "
import sys, json
t = sys.stdin.read(); d = json.loads(t[t.index('(')+1:t.rindex(')')])
print('vegReady =', d.get('vegReady'))
seats = d.get('seats', [])
print('有 veg 欄的席位數 =', sum(1 for s in seats if 'veg' in s), '/', len(seats))
print('素食席位數 =', sum(1 for s in seats if s.get('veg')))
"
```

**對照組（沒有這一行，數字不算數）**：同一場活動打 `getActivityStats`，看它的 `counts.veg`：

```bash
curl -s "<GAS_URL>?action=getActivityStats&token=<BOARD_TOKEN>&act=midyear2026&callback=cb" \
  | python3 -c "
import sys, json
t = sys.stdin.read(); d = json.loads(t[t.index('(')+1:t.rindex(')')])
print('統計頁的素食人數 =', d.get('counts',{}).get('veg'))
"
```

Expected: **兩個數字必須一致**（統計頁的 `counts.veg` 只算同仁，所以比對的是 `getSeatingBoard` 裡 `kind=='emp'` 的素食數；來賓要另外看）。不一致就是我的算法錯，不是資料錯——回頭查，不要改資料。

- [ ] **Step 3: 合併前端分支並發布**

```bash
cd ~/Projects/jdc-line-liff
node --test tests/*.test.js                        # 綠才往下
node tests/manual/veg-ui.mjs                       # console 零錯誤才往下
git checkout main && git merge --no-ff feat/veg-seating
git push
```

- [ ] **Step 4: 開真的頁面驗收**

用真 token 開 `stats.html?t=<token>` → 桌次管理分頁：

1. 磚上有「素 N」
2. 點一桌，明細有綠色「素」標
3. 「🥬 素食」pill 列得出全部素食者，未排桌者標「未排桌」
4. 彙總卡的合計 = Step 2 那兩個對照數字加總（同仁＋來賓）
5. **看 console 有沒有錯誤**（memory `feedback_open_page_is_not_verification`：沒看 console 等於沒驗）

- [ ] **Step 5: 用真資料下載兩份檔案各開一次**

- 排位用檔：分類名單裡素食者名字有「（素）」
- 正式座位表：座位格**沒有**「（素）」、底部有素食彙總段

- [ ] **Step 6: 回頭把「素食人數」欄告訴各單位**

來賓名單的舊格式檔沒有這欄，一律當 0。要讓份數正確，下次收來賓名單時要用帶「素食人數」欄的新格式。
⚠️ 這是**使用者動作**（要發給各單位），不是程式能自己完成的——完成前，彙總卡的來賓份數一律是 0，不要當成「沒有來賓吃素」。

---

## Self-Review

**Spec coverage：**

| Spec 段落 | 對應 Task |
|---|---|
| §4.1 seat 帶 `veg`、判定唯一 | Task 1、2 |
| §4.2 來賓表「葷素」欄＋上傳檔「素食人數」 | Task 2（表）、Task 3（上傳檔） |
| §4.3 `vegReady` 就緒旗標 | Task 2（產出）、Task 6／7（消費） |
| §5.1 磚與明細列標記 | Task 5 |
| §5.2 第三顆 pill | Task 6 |
| §5.3 常駐彙總卡 | Task 7 |
| §5.4 兩份 Excel | Task 8 |
| §6.1 同家廠商跨桌誤差要顯示在畫面上 | Task 4（`splitVendors`）、Task 7（琥珀提示） |
| §6.2 分母只含「已確認參加」 | 由 `getSeatingBoard` 既有的 `出席=參加` 篩選決定，無需改動；Task 9 Step 2 的對照組會驗到 |
| §6.3 舊格式檔當 0 | Task 3（`ix('素食人數')<0` 走 0）、Task 9 Step 6（告知各單位） |
| §7 測試 | Task 3、4、8 的 TDD 步驟；UI 走 Task 5 的 harness |
| §8 部署順序與對照組驗收 | Task 9 |
| §9 分支 | Global Constraints ＋ Task 1 Step 1、Task 3 Step 1 |

**未在 spec 但實作必要的補充（已寫進計畫）：**
- Task 6 Step 4 改 `renderDetail` 的未排桌顯示——原本未排桌者不顯示任何桌號膠囊，在素食視圖裡看起來像漏資料。
- Task 5 的 UI 驗收 harness——spec §7 只寫「必須實際開頁面看過」，沒寫怎麼做。

**Type consistency：** `veg`（boolean，seat 上）／`vegReady`（boolean，回傳頂層）／`vegSummary()` 回 `{rows,total,splitVendors}`／`expandGuestRow()` 回 `[{seatNo,veg}]`。三個消費點（Task 5、6、7、8）用的都是這些名字，無分歧。

**已知限制（不是缺口，是選擇）：** `Code.js` 沒有單元測試，Task 2 的 `isVegDiet` GAS 全域解析路徑只能在 Task 9 Step 2 的 live 驗證抓到。這與 `roles-gas-global.test.js` 註解講的是同一類缺口，但為一個 5 行的判定函式建 vm 載入環境不相稱（`Code.js` 依賴 `SpreadsheetApp`，vm 載不動）。
